# Sentinel Spec — production dependencies
# Pin minor versions; patch versions float to pick up security fixes.

# IBM Watsonx AI SDK
ibm-watsonx-ai==1.5.14

# HTTP framework + server
fastapi==0.135.3
uvicorn==0.44.0
starlette==1.0.0
python-multipart==0.0.24

# Pydantic (FastAPI data validation)
pydantic==2.12.5
pydantic-settings==2.13.1

# Async + HTTP client (pulled in by ibm-watsonx-ai and FastAPI test client)
anyio==4.13.0
httpx==0.28.1
httpx-sse==0.4.3
